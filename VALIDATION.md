# Portfolio validation

## Deployment preparation — 20 September 2026

**Ready to upload as a public preview; no external deployment was performed.**

- Added `npm run build:deploy` to run deployment preflight, Node tests, the production build, and a new compiled-output checker. Public preview builds preserve `preview: true` and all existing verification flags. Indexed releases still require final content review; unresolved project years, freelance slots, and testimonial prompts now fail that gate explicitly.
- Centralized `SITE_ORIGIN` and `BASE_PATH` resolution for Vite, metadata, postbuild output, and checks. The GitHub workflow takes its host and repository path from `configure-pages`; root hosting remains the local default. Added Netlify build settings and a [deployment guide](docs/DEPLOYMENT.md) covering Cloudflare direct uploads without GitHub, Netlify, and GitHub Pages. No dependency, page design, content claim, hosting account, or repository visibility changed.
- Preview builds retain HTML `noindex`, restrictive robots rules, and no sitemap. Generated `_headers` adds the matching response directive on Cloudflare/Netlify. Release postbuild excludes Continuum from the sitemap and keeps its separate noindex rule. A regression confirms stale release sitemaps are removed when rebuilding preview metadata. Indexing directives do not restrict access to a publicly deployed URL.
- **60 Node tests pass**, including nine deployment regressions covering settings, URL/base validation, preview/final-release gates, missing résumé files, local asset/fragment checks, accessible ID references, and generated indexing rules. Both complete deployment builds pass: **942 local references across 26 HTML files** for `/`, and **961 references across 26 HTML files** for `https://sumit-nikhare.github.io/portfolio-website/`. GitHub canonical and social-image URLs were checked separately. The existing lazy Three.js chunk warning remains.
- Local static HTTP smoke checks returned **200 for all 26 HTML routes plus four representative assets/downloads in each hosting layout** (60 successful responses total), including direct case-study URLs. HTML, PDF, and font content types were checked. Temporary smoke servers were closed. This verifies local static delivery, not provider configuration, browser rendering, or interactivity.
- Created `artifacts/deployment/portfolio-preview.zip` from the final root build: **164 files, 2,764,883 ZIP bytes**, with `index.html` at the archive root. ZIP integrity and every archived file's bytes match `dist`; there is no source tree or repository metadata in the upload. The largest file is 537,087 bytes. File count and per-file size fit Cloudflare dashboard upload limits. Build logs, the package checksum, GitHub-path output, and HTTP results are retained beside the ZIP; `artifacts/` stays ignored by Git.
- **Browser visual review, execution of the 96 prepared browser scenarios, and current Lighthouse measurements remain pending** because no browser was available in the latest environment discovery. No new accessibility or performance score is claimed. Nothing was committed, pushed, or published during this preparation.

## Site-wide quality pass — 19 September 2026

**Source fixes and automated checks complete; visual/browser sign-off remains pending.**

- Motion preferences now synchronize with other tabs and re-read the saved choice when a page returns from the back/forward cache. Labels and pressed states update together. Unchanged preferences do not restart animation setup; unavailable storage preserves the current in-memory choice.
- Collection animations settle before page exit. Work and Blog restore their filter/search/view state from the current URL on history events; finished layout changes request a fresh measurement. Freelance placeholders remain separate from employment filtering and counts.
- SVG playback finishes cleanly on page exit or a motion-preference change, with a completed path and consistent disabled Pause control. Switching to Full motion no longer starts unsolicited playback. Hidden tabs pause the current trace until the visitor resumes or replays it.
- Shared links, buttons, Work previews, and Blog previews now provide matching keyboard-focus feedback. Pale demo/component surfaces and the résumé use a dark focus outline. Small onboarding captions changed from approximately **4.15–4.17:1 to 5.02:1** against their declared pale background; the focus outline changes from approximately **1.02:1 to 11.54:1**. These are calculated source-color ratios, **not browser contrast/a11y audit results**.
- The image viewer’s mobile controls, zoom reset, section-index links, Back to top, and onboarding actions have at least 44 px control heights; mobile image-viewer icon buttons also have 44 px widths. Editable mobile text fields use 16 px text. The closed menu’s preview image is lazy-loaded, decoded asynchronously, and given low fetch priority. The Lighthouse script’s default case-study target now points to Continuum instead of the legacy Forma redirect. No new dependency, font, imagery, WebGL scene, or published content was added.
- **51 Node tests pass**, including six new regressions covering motion history/storage synchronization, blocked storage, collection URL/history cleanup, SVG preference/page-exit cleanup, and pause/resume/completion behavior. The new interaction tests execute the real modules with event/DOM/animation doubles; they do not certify rendering or actual browser timing.
- Root and `/portfolio/` production builds pass. Static checks resolve **936 local references across 26 root HTML files** and **929 references across 19 subdirectory HTML files**. All content pages retain one H1, resolving ARIA references, sized/described content images, static navigation, and preview noindex. **24 HTML source files are unchanged**; only shared menu-image loading attributes changed in the remaining partial. The established sections, copy, real-project counts, freelance placeholders, and sample labels remain intact. The existing lazy Three.js chunk warning remains.
- Added browser regressions for synchronized motion choices/history and pale-surface focus/mobile controls. All **96 browser scenarios list successfully**, but were **not run**. Screenshots, actual mobile/tablet/desktop layout, animation timing, visual crops, both-theme keyboard review, axe audits, and Lighthouse performance/accessibility measurements still require a connected browser. No new scores are claimed.
- The prior local-preview restriction no longer blocks this phase: startup initially hit sandbox `EPERM`, then automatic approval allowed the same local preview command. The server is running at `http://127.0.0.1:4173/`, and approved HTTP smoke checks return **200 for all 19 current page routes**, including direct case-study and article URLs. Browser skill setup reported **“No browser is available”**, and its documented discovery returned an empty list. No alternate browser-control method was used. The remaining blocker is browser availability, not a new credit rejection. Nothing was published.

## Playground: three-part reframing brief — 19 September 2026

- Implemented the brief’s exact section order: **Introduction → Experiment Grid → Experiment Detail**, visibly numbered 01–03 and identified by `data-playground-section`. Four previews link to the existing onboarding, typography, component, and SVG experiments. Each detail has visible Idea / Experiment / Result notes, a working view, and a return link. Category labels describe the existing studies; no empty categories or invented professional results were added.
- Built the previews with editable HTML/CSS and inline SVG. `src/playground.css` is loaded only by Playground, reuses the existing theme tokens, and adapts the grid and supporting notes for smaller screens. Shared typography, navigation, cursor, themes, motion settings, and the nonblocking entrance remain intact. The page uses existing heading reveals and a small link-arrow response; it adds no new animation module, library, raster asset, WebGL, or pinning.
- Kept all existing experiment controls, values, static fallbacks, and homepage destinations. Product headings now sit at level four beneath each experiment’s level-three heading. The onboarding module uses `[data-demo-heading]` for its existing focus behavior instead of a fixed heading tag. Experiment state and behavior are otherwise unchanged.
- All **45 Node tests pass**. Root and `/portfolio/` production builds pass. Static validation resolves **936 local references across 26 root HTML files** and **929 references across 19 subdirectory HTML files**. It checks section order and labels, four linked previews/details, twelve visible narrative notes, heading and ARIA targets, fallback/control markup, stylesheet isolation, and existing control bindings. **24 protected source files remain unchanged**, including Home, About, Work, all real case studies, Continuum, shared theme/settings/navigation, and the playground interaction module. Preview remains enabled; nothing was published. The existing lazy Three.js chunk-size warning remains.
- Updated existing browser scenarios for the new section structure, keyboard grid navigation and return focus, direct fragment refresh, no-JavaScript content, and responsive Playground checks. All **94 browser scenarios parse/list successfully**; they were **not executed**. Live interactions, responsive appearance, both-theme contrast, motion, accessibility audits, and Lighthouse measurements remain pending.
- Automatic approval review previously rejected browser/local-preview startup because workspace credits were exhausted. No new browser launch, alternative launch, or live preview server was attempted in this phase.

## Work: featured, freelance, additional, and archive — 19 September 2026

- Reframed `/work/` into the agreed five sections: Introduction → Featured Projects → Freelance Projects → Additional Work → Archive. Featured contains CHIP-2, Samasta, and CHO Soft; compact additional summaries contain AI-enabled dashboard and ABDM–ABHA; smaller archive rows contain Anarock and TARP from the verified 2022 Permute employment period. Each employment entry identifies its domain, employer, problem, contribution, visual, and project year “To confirm.” The page identifies the existing project pages as résumé-based summaries.
- Added three explicitly labeled freelance preview slots for the 2–3 projects Sumit confirmed. Project/client, role/scope, deliverables, and year await supplied details. They have no invented client names, results, artwork, or inactive project links. A real contact CTA supports freelance enquiries. Preview slots stay outside the seven documented employment-project count, category filters, and real-project navigation loop. No real projects were reclassified as freelance work.
- Extended the shared collection module to hide empty employment groups when filtered. Freelance previews remain visible. The existing URL-based category and list/grid state is preserved; grid/list applies to featured projects and becomes disabled when no featured entry matches. Additional work and archive stay compact in either view. The Blog collection retains its ungrouped behavior. Existing animation code and cleanup remain in use; no graphics dependency or asset was added.
- **45 Node tests pass**, including four new checks that run the collection module with DOM/animation doubles: group visibility and independent freelance previews, direct filtered/list URL restoration, repeated filters and reduced-motion state, and Blog search/empty/reset behavior. These are state checks, not verification of browser animation or layout.
- Root and `/portfolio/` production builds pass, retaining the existing lazy Three.js chunk-size warning. Static validation resolves **926 local references across 26 root HTML files** and **919 references across 19 subdirectory HTML files**. It confirms the five-section order, 3/2/2 employment grouping, all seven project links and metadata, preview labels and count separation, accessible ID references, and contact link. Formatting and `git diff --check` pass.
- Source hashes confirm **20 protected pages/shared files** are unchanged, including Home, About, all seven real projects, Continuum, Playground, Résumé, Contact, Blog index, shared header/footer, theme values, sculpture, and cursor. Continuum stays featured on Home and outside this Work collection. Preview mode remains enabled; nothing was published.
- Updated existing browser scenarios for the Work structure, filter counts, empty-group visibility, independent freelance previews, layout-control disabled state, contact navigation, history, and no-JavaScript content. All **94 scenarios list successfully; they have not been executed for this update**. Responsive appearance, both-theme contrast, real keyboard/focus behavior, animation transitions, browser accessibility, and Lighthouse measurements remain pending.
- Browser/local-preview startup remains blocked by the earlier automatic approval rejection for exhausted workspace credits. No alternative launch was attempted. README, page map, provenance notes, research implementation note, and sample guide now describe the Work structure and freelance editing workflow. Playground reframing is next.

## About: professional and personal reframing — 19 September 2026

- Reframed `/about/` in the brief’s order: Hero → Professional Story → Experience → Capabilities → Beyond Work → Résumé → Contact. Recognition is intentionally omitted under the brief’s “Remove this section if weak” provision because no meaningful verified recognition was supplied. The seven visible sections are numbered consecutively; source comments retain the original S1–S8 mapping.
- Kept the existing headline, portrait composition, Space Grotesk/Inter typography, serif accent treatment, theme colors, editorial grid, and shared motion. Added a short professional story; preserved all four verified role/company/date combinations and summarized each role’s scope. Capabilities now use Product, Experience, Craft, Systems, and Collaboration. Unverified product-strategy, roadmap-contribution, and mentoring claims remain out pending the optional clarification.
- Beyond Work includes the confirmed anime interest and résumé-backed communication/motion work using the existing poster treatment. A separate résumé section links to the current online page and PDF. The smaller contact closing supports roles and freelance work. Philosophy remains owned by Home; About links to it. All old About anchors are retained, including `#principles`.
- Added only scoped editorial CSS and reused existing reveals. No new JavaScript module, graphics dependency, pinning, generated image, or public asset was needed. Source hashes confirm **39 protected files** are unchanged, including Home, all project pages, Work, Playground, Résumé, Contact, animation modules, shared navigation, theme values, and motion configuration.
- All **41 existing Node tests pass**. Root and `/portfolio/` production builds pass with the existing lazy Three.js chunk-size warning. Static validation resolves **920 local references across 26 root HTML files** and **913 references across 19 subdirectory HTML files**, checks named sections, exact role/date combinations, capability groups, preserved anchors, readable static markup, portrait attributes, PDF download, contact links, and accessible ID references. Formatting and `git diff --check` pass.
- Updated existing browser checks for the new About order, capability groups, résumé access, no-JavaScript content and story anchor, and light-theme responsive coverage. **94 browser scenarios parse and list successfully; they were not executed.** Current responsive appearance, real keyboard behavior, theme contrast, scroll reveals, accessibility audits, and Lighthouse scores remain unmeasured. An older portrait screenshot was consulted only as an established-design reference, not as verification of this update.
- Browser/local-preview startup remains pending under the previous automatic approval rejection for exhausted workspace credits. No alternative launch or publication was attempted. Preview mode remains enabled. README, page map, source notes, research implementation note, and sample guide reflect the completed About phase; Work and Playground are still pending.

## Continuum featured on Home — 19 September 2026

- Replaced the first homepage card, CHIP-2, with Continuum at the user’s request. The card uses the existing SVG hero at its native 8:5 ratio, labels the work as a fictional sample, and links to both the case study and referral demo. Its metadata reads “Sample scope” and “2026 · Sample”; no fictional result is presented as a professional achievement.
- Samasta and CHO Soft remain the two other homepage cards with years “To confirm.” CHIP-2 remains in the seven-project Work collection and its original navigation loop. All project-page source files, including Continuum, are unchanged; preview mode and the sample’s noindex/sitemap exclusion remain in place.
- Production build and all **41 Node tests pass**. Static checking resolves **918 local references across 26 built HTML files** and verifies the card order, fictional labels, artwork, demo anchor, years, and retained Work entry. Updated existing browser scenarios for Continuum’s cover interaction and Home → sample → history navigation; all **94 scenarios list successfully**, but were not executed.
- Browser visual review remains pending under the earlier automatic approval rejection of browser/local-preview startup for exhausted workspace credits. Nothing was published.

## Homepage: nine-part reframing brief — 19 September 2026

- Reframed Home in the brief’s exact order: Hero → Selected Work → Professional Snapshot → Design Philosophy → Playground Preview → Testimonials → About Teaser → Contact CTA → Footer. The eight main sections have separate anchors and visible numbered labels; the shared footer completes the ninth part.
- Selected Work now contains CHIP-2, Samasta, and CHO Soft. Every card includes its number, title, problem, domain, contribution, visual, case-study link, and **Year: To confirm**, as explicitly approved by Sumit. CHIP-2 and Samasta include source-qualified numerical indicators and context links. CHO Soft uses a scope indicator without inventing an outcome. The lead project spans the grid, followed by two supporting projects.
- Kept the professional snapshot concise. Moved the existing three design principles from About to Home and replaced the duplicated About section with a link. The three playground previews lead to the existing typography, component, and SVG experiments; the typography preview retains its keyboard-operable weight control. Two testimonial placeholders explicitly identify themselves as writing prompts, not endorsements. The About teaser uses the verified portrait and confirmed anime interest. Contact supports roles and freelance enquiries with Email, LinkedIn, and résumé access; the shared footer now includes direct Email and LinkedIn links.
- Preserved the existing typography, theme tokens, hero specialty sculpture, custom cursor, navigation, motion controls, and nonblocking wordmark entrance. The shallow capability unfold now supports the philosophy columns using renamed selectors/configuration with unchanged motion values. Existing cleanup and reduced-motion behavior remain in the module. No new library, WebGL scene, or additional pinning was introduced.
- All **41 Node tests pass**. Root and `/portfolio/` Vite builds pass with the existing lazy Three.js chunk-size warning. Static validation resolves **920 local references across 26 root HTML files** and **913 references across 19 subdirectory HTML files**, checks unique IDs and accessible references, and confirms all nine brief parts, the three project cards and experiment destinations, placeholder labels, contact/footer links, and approved years. Formatting and `git diff --check` pass.
- Source hashes confirm that Work, Playground, Résumé, Contact, all seven real project pages, and the Continuum sample remain unchanged. The complete hero-stage markup is also unchanged. Work still contains seven projects; the sample remains outside the project counts and retains `noindex`. Preview mode stays enabled; nothing was published.
- **94 browser scenarios parse and list successfully**. Updated checks cover the nine-part structure, project years/counts, preview navigation, typography keyboard control, shared footer links, no-JavaScript content, and philosophy/snapshot cleanup during reduced-motion and viewport changes. They have **not been executed for this update**. Responsive appearance, both-theme contrast, real keyboard and motion behavior, accessibility audits, and Lighthouse measurements remain pending.
- Automatic approval review previously rejected browser and local-preview startup because workspace credits were exhausted. No alternative launch was attempted; no live review server was started. The README, page map, content provenance, and sample authoring guide now describe the current Home phase and the still-deferred full About, Work, and Playground restructuring.

## Continuum: fourteen distinct sections — 18 September 2026

- Corrected the sample to present the original brief's fourteen case-study sections individually, in its exact order: Project Hero → Executive Snapshot → Context + System → Problem + Stakes → Success Definition → Designing for Reality → Evidence + Reframing → Key Product Decisions → Final Experience → System Details → Validation + Iteration → Impact → Reflection + Next Bet → Next Project. Every section has a visible numbered label, a heading, its own anchor, and one optional guide note. These sections remain identifiable with the guide off and disclosures closed.
- Kept the five compact navigation shortcuts and added their section ranges. The index can now map each separate section to its parent navigation group; existing project/article behavior still falls back to the section's own ID. Existing sample chapter URLs remain valid.
- Made the evidence summary and delivery-state explanation/illustration visible in their own sections. Added concise fictional context and urgency, and separated reflection into what worked, what to change, uncertainty, and the next validation step. Main editorial content is **795 words**, with **751 words** in optional supporting disclosures.
- Root and `/portfolio/` production builds pass. Static checks resolve **914 local references across 26 root HTML files** and **907 references across 19 subdirectory HTML files**. The fourteen section labels were compared directly with S1–S14 in the supplied brief; their order, independent section structure, headings, anchors, and one-to-one guide-note mapping pass. The existing lazy Three.js chunk-size warning remains.
- All **41 existing Node tests pass**. Formatting and `git diff --check` pass. The updated sample browser file lists **nine scenarios**, including visible section order and direct section URLs with compact navigation tracking; browser execution remains pending. The Node tests do not verify the new browser layout or navigation highlighting.
- Hash checks confirm Home, About, Work, Playground, all seven real project pages, and both sample demo modules are unchanged by this correction. Preview mode, literal sample `noindex`, the demo, shared themes, cursor, gallery, and wordmark entrance are retained. The authoring guide, README, and page map now distinguish content sections from navigation shortcuts.
- Browser visual/accessibility review remains pending because automatic approval review previously rejected browser and local-preview startup for exhausted workspace credits. No new browser launch or publication was attempted.

## Continuum sample and nonblocking entrance — 18 September 2026

- Added `/projects/sample-continuum/` with fourteen case-study topics grouped under Overview, Problem, Decisions, Solution, and Impact. The main editorial content is approximately 670 words, with approximately 800 words of optional supporting evidence. Research, an eight-week timeline, team, testimonial, and the 4/8 → 7/8 and 150 → 105-second comparison are explicitly fictional. The sample's next-project link goes to CHIP-2.
- Added fourteen optional structure notes and `docs/SAMPLE-CASE-STUDY-GUIDE.md`. The guide has an accessible pressed state, retains focus/reading position, and refreshes scroll measurements. Native closed disclosures remain available without JavaScript. The sample module also remeasures after individual note toggles and demonstration changes.
- Added a self-contained referral demonstration with required destination selection, reversible review, online confirmation, an explicitly labeled offline queue, one-time reconnection, and restart. State stays in memory; no personal data, accounts, persistence, network submission, or clinical recommendations. A static walkthrough remains available without the app or sample bundle.
- Created five original SVG illustrations with embedded local fonts: desktop/mobile hero, three-role system map, annotated before/after comparison, and delivery-state model. All five were rendered with native canvas and inspected for clipping and legibility; a mobile footer-caption overlap was corrected. This is artwork inspection, not browser layout review. An editable HTML queue and HTML validation comparison keep essential information available as text.
- Removed the blocking loader and old visit schedule from bootstrap, markup, motion, theme, cursor, sculpture, and screenshot integrations. A 0.65-second wordmark-symbol entrance runs with the ordinary page entrance. Reduced motion skips it; no content locking, visit counting, or delayed release remains.
- **41 Node tests pass**, including five referral-state and three bootstrap regressions. Production builds pass for `/` and `/portfolio/`. Static checks resolve **913 references across 26 root HTML files** (including legacy redirects) and **906 references across 19 subdirectory HTML files**. All five chapters and fourteen guide notes are present; no duplicate IDs, unresolved build placeholders, or loader markup were found. Formatting and `git diff --check` pass. The existing lazy Three.js chunk-size warning remains; the sample adds no WebGL scene.
- Source hashes confirm the homepage, seven real projects, Work, About, and Playground are unchanged by this task. Preview mode remains enabled. An isolated release-style postbuild check lists only the existing seventeen content pages in its sitemap, excludes Continuum, and preserves the sample's literal `noindex, nofollow`.
- **91 browser scenarios parse and list successfully**, including eight sample scenarios and updated entrance, theme, and scene checks. They have **not been executed for this delivery**. Both-theme layouts at 390, 834, 1440, and 1920 px, real keyboard/focus behavior, interrupted motion/disclosures, history, browser accessibility, and mobile Lighthouse targets remain pending. Earlier browser results below do not verify this update.
- Automatic approval review previously rejected browser launch and rejected starting the local preview server again during this delivery because workspace credits are exhausted. No alternative launch was used. A live review server is not running from this session. To review locally, run `npm run preview -- --port 4173` and open `http://127.0.0.1:4173/projects/sample-continuum/`. Nothing was published.

## Smoother profile handoff and capability depth — 18 September 2026

- Replaced the profile facts' three independent scrub animations with one shared progress value. Adjacent highlights now exchange intensity through a continuous curve whose velocity and acceleration reach zero at each phase boundary. The total highlight weight stays at one through the handoffs, before fading out completely.
- Reduced the profile lift to 14 px of depth, 0.8 degrees of pitch, and 1.5 px vertically. Removed animated text colors and per-phase transform switching. The light and lift share a longer 0.6-second scroll catch-up over the whole list, with stable parent measurements.
- Added a coordinated 3D unfold to “How I can help.” The three capability columns begin at a four-degree pitch, 44 px behind the reading plane, and 24 px lower, then settle into their original grid from left to right. A fine accent rule appears during the unfold and fades at rest. The text remains visible, no scroll pinning is added, and both themes use their existing accents.
- Existing media-context cleanup covers both effects when motion preferences, breakpoints, or page lifecycle change. Mobile/coarse pointers and reduced motion retain the static layout; keyboard focus flattens the section and print removes decorative effects.
- The production build and **33 Node tests pass**, including four new regression checks for highlight endpoints, conserved handoff weights, boundary continuity/velocity, reverse-scroll determinism, and different row counts. These exercise the actual shared highlight function, not browser rendering. Formatting and `git diff --check` pass. The existing lazy Three.js chunk-size warning remains.
- Browser smoothness and responsive visual review remain pending: the earlier automatic approval review rejected browser launch for exhausted workspace credits. No current visual, accessibility, or performance measurements are claimed.

## Profile facts: sequential scroll highlight — 18 September 2026

- Replaced the three facts' entrance tilts with a reversible scroll highlight: Currently → Experience → Based in. Each row gains a soft theme-colored wash, an illuminated top rule, and tinted primary text while lifting 24 px in depth with a 1.5-degree pitch. It returns to the flat, unhighlighted state afterward. The heading and introduction retain their earlier entrance.
- Each row's phase uses its layout offset within the stationary list and a common viewport reading line. The highlight range adapts to row height, with slight overlap between adjacent rows. No additional pinning, idle animation, assets, or hidden text. Existing motion/breakpoint/history cleanup applies; mobile and reduced motion remain static, and print removes the decorative effects.
- The production build, all **29 existing Node tests**, formatting checks for the three changed source files, and `git diff --check` pass. The existing Three.js chunk-size warning remains. The Node tests do not exercise this new browser effect.
- Browser scroll and visual review remain pending because the earlier automatic approval review rejected browser launch for exhausted workspace credits. No new browser or performance measurements are claimed.

## 60-second section scroll depth — 17 September 2026

- Added a restrained desktop scroll entrance to the profile heading, introduction, and three facts: 36 px of depth, a four-degree pitch, and 16 px of lift settle into a flat reading plane. The facts enter with a small scroll stagger. Content stays visible throughout and the existing document flow is unchanged.
- Static parents provide the scroll bounds. The same scrubbed tweens handle forward and reverse scrolling, and settled text loses its transform. The existing media context reverts animation on reduced-motion selection, smaller/coarse-pointer layouts, and page exit, and restores it on history return. CSS keeps focused section content and printed content flat. No new assets, canvas, or animation dependencies were added.
- `npm run build` and all **29 existing Node tests pass**; these tests do not validate the new browser animation. The production build retains the existing lazy Three.js chunk-size warning. `git diff --check` passes.
- Browser scroll/visual review remains pending because the earlier automatic approval review rejected browser launch for exhausted workspace credits. No new browser, accessibility, or performance measurements are claimed.

## Homepage structure and navigation — 17 September 2026

- Reorganized the homepage into six sections: the existing three-specialty hero, four selected projects, About with the 60-second snapshot, three capabilities, a compact interactive typography preview, and Contact. Each project now states its workflow and individual contribution. Existing covers remain labeled workflow illustrations.
- Removed the homepage ticker, redundant overview links, large decorative supporting artwork, philosophy accordions, and empty testimonial section. Kept the hero sculpture, custom cursor, theme and motion settings, and mandatory five-second loader schedule.
- Simplified all 18 shared headers to the name and Menu button. All six page links remain in the existing full-screen menu. A native disclosure provides the same links when JavaScript is unavailable or menu initialization fails.
- Added a font-weight control to the homepage playground preview, reusing the existing playground module. Supporting sections use the existing dark and light palettes, fine rules, and responsive column layouts.
- The production build and all **29 Node tests pass**. Static validation checked **867 local links, assets, and anchors across 25 built HTML files**, including legacy redirects; all resolve. Header structure, section order, project count, and typography markup also pass static checks. `git diff --check` passes. The existing dynamically loaded Three.js chunk-size warning remains.
- Updated browser scenarios for the native fallback menu and the new homepage order and typography control. All **49 scenarios** in the scene, portfolio, and theme files parse and list successfully; they have not been executed for this update. Browser execution and responsive visual review remain pending because the earlier automatic approval review rejected browser launch for exhausted workspace credits. No new browser, accessibility, or performance measurements are claimed.

## Three-specialty hero — 17 September 2026

- Kept the original hero-only tilted panel sculpture and its pointer response. Replaced the five project-image surfaces with three specialty cards: Product & UX design, Interaction design, and Design systems. The sequence is now Assemble → Unfold → Resolve, with three separate front-facing cards at the end.
- Shortened the entrance to 0.9 seconds and the scroll distance from two viewport heights to 0.75. The four-project Selected Work section, custom cursor, and mandatory five-second loader schedule are preserved.
- Specialty copy and SVG symbols live in accessible HTML. Canvas textures use the same content and local fonts, redraw with the theme, and are disposed with the scene. Mobile, reduced motion, JavaScript-disabled, and WebGL-unavailable layouts retain native HTML cards.
- All **29 Node tests pass**. Updated the scene checks for the three-card ending, non-overlap, continuity, reversibility, settling, and camera bounds at 101 scroll positions across four aspect ratios and three pointer positions. The production build passes with the existing dynamically loaded Three.js chunk-size warning.
- Rendered and visually inspected the actual card drawing function in dark and light palettes using native canvas; the contact sheet is `artifacts/specialty-hero/card-review.png`. This reviews card artwork, not browser layout or WebGL rendering.
- Updated the six prepared browser scenarios for the shorter sequence and specialty fallback. Browser execution and responsive visual review remain pending because the earlier automatic approval review rejected browser launch for exhausted workspace credits. No new browser, accessibility, or performance results are claimed.

## Custom cursor — 17 September 2026

- Added a custom dot and fine ring across desktop pages, an accent ring over controls, a VIEW arrow circle over project/gallery links, and a slim caret over editable fields. Shape changes take 180 ms; pointer position follows directly without a trailing or idle animation. Theme colors and reduced-motion choices apply to the cursor.
- A manual, noninteractive popover keeps the decorative layer above native dialogs. SVG cursors provide a visible custom fallback before the app starts, during snapshots, or if the popover API fails. Touch and forced-color modes retain platform behavior. Cursor cleanup handles focus loss, page exit/history, dialog changes, and keyboard navigation.
- All **29 Node tests pass**, including seven new cursor lifecycle checks using DOM/event doubles. These cover single-frame tracking, stationary-pointer scroll updates, unavailable/throwing APIs, touch and capability changes, click cleanup, dialog layer changes, and keyboard/page visibility/history cleanup. They do not validate browser rendering or real CSS hit-testing.
- The production build passes, and a separate `/portfolio/` base-path build also passes. Five browser scenarios are prepared and listed successfully in `tests/browser/cursor.spec.js`.
- Browser execution and visual verification remain pending: the earlier automatic approval review rejected browser launch because workspace credits were exhausted. No updated visual, accessibility, or performance measurements are claimed.

## Homepage experiment rollback — 16 September 2026

- Restored the 14 files changed by the full-page Living Interface experiment to their earlier state, using the recorded edit history. Replaying that history reproduced the rejected version before restoration, confirming the rollback scope.
- Removed the persistent full-page canvas, eight chapter transformations, and narrowed content layout. Restored the earlier hero sculpture, artwork depth effects, and original homepage section layouts. The four-project grid, profile content, theme switch, loader policy, and earlier animation lifecycle fixes remain in place.
- The restored production build and all 22 Node tests pass. The existing dynamically loaded Three.js chunk warning remains. Browser visual verification is still pending because the earlier automatic approval review rejected browser launch for exhausted workspace credits.
- Alternative minimal motion directions are suggestions only; no replacement animation design has been applied.

Browser review below was completed on 13 September 2026 against the local production build, before animated theme switching and the four-project homepage layout were added. Subsequent source and build checks were completed on 14 September 2026; browser visual review of those changes remains pending.

The 16 September 2026 update adds the homepage 3D scroll sequence and animation lifecycle fixes. Its local results are recorded separately below; earlier browser results are not measurements of this update.

## Functional and visual checks

- Production build passes; Vite discovers all 18 HTML pages (17 content pages plus the 404 page) automatically.
- All 22 Node tests pass: four existing interaction checks, twelve theme-animation lifecycle checks, and six sculpture-sequence checks.
- All 50 browser checks pass in one regression run against the personalized portfolio, including ten motion lifecycle checks. Functional checks use intervening visits; dedicated loader checks exercise the actual five-second holds on visits 1, 6, and 11.
- Chrome checks cover five homepage viewport widths: 360, 390, 834, 1440, and 1920 px. The five new page types and an article also pass layout checks at 360, 834, 1440, and 1920 px.
- Tested native-dialog keyboard operation, Escape, focus return, menu previews, gallery boundaries and zoom, explorer keyboard navigation, demo validation/back/restart, all playground controls, and the full signature scatter-and-return cycle.
- Tested direct case-study URLs, reload, browser history, JavaScript-disabled content, and WebGL-unavailable artwork fallback.
- Confirmed desktop canvas initialization and scroll pinning, removal of graphics in reduced motion, remembered preferences, and cover distortion settling after pointer movement stops. Offscreen and hidden-tab rendering guards are implemented in the graphics module.
- Automated axe checks pass on all 17 content pages and the media dialog. An explicit visible-label/accessibility-name check also passes.
- All 770 local navigation and asset references in the personalized root build resolve, including the résumé PDF and seven legacy project redirects. A previous build also verified the `/portfolio/` base path (753 references before personalization).
- The expanded checks also verify all seven projects and their navigation loop, category filters, grid/list state across refresh/back, article search and empty states, clipboard interactions, contact validation/reset/no delivery claim, and résumé print styling.
- Dependency installation reported zero known vulnerabilities.

Final screenshots and browser traces are generated under `artifacts/`, which is excluded from source control.

## 3D sequence and animation review — 16 September 2026

- Added four reversible sculpture stages (Assemble, Explore, Connect, Resolve), two orbital guides, a stage indicator, and a four-panel ending over two viewport heights. Project-cover wrappers and the About/Playground artwork gain scroll depth and pointer tilt on desktop with full motion.
- Fixed the sculpture entrance running behind the loader, empty first canvas frames, unconditional idle rendering, stale texture callbacks after page exit/visibility changes, short-screen pin removal, and context-loss cleanup. The camera adapts to the available scene aspect ratio; the ending is front-facing regardless of pointer position.
- Reviewed entrance, menu, disclosure, tab, collection, SVG, signature, theme, and scroll lifecycles in source. Added missing completion/style cleanup, guarded asynchronous menu previews, settled interrupted tab heights and component animations, and replaced the ticker's partial repeat with two identical groups and an exact loop distance.
- `npm run check`: 22 passing Node tests. Six new tests cover stage/end states, reverse-scroll determinism, continuity at pose boundaries, exact settling without overshoot, smoothing at 30/60/120 Hz, and Three.js projection of visible panel corners at 101 scroll positions across four scene aspect ratios and three pointer positions. Projection uses real Three.js math without a GPU; it does not test browser rendering.
- The final production build passes with the existing large Three.js chunk warning. Static validation of the built homepage confirms four projects, four independent depth wrappers, four stage labels, identical ticker groups, and 75 resolving local references.
- Six browser scenarios are prepared in `tests/browser/scene.spec.js`: forward/reverse stages, loader timing, resizing and motion changes, context loss, card cleanup, and ticker continuity. The suite can be listed without launching Chrome; execution and visual review remain pending because the earlier automatic approval review rejected browser testing for exhausted workspace credits.

## Homepage Selected Work

- Replaced the staggered three-project layout with two equal columns featuring CHIP-2, Samasta, CHO Soft, and the AI-enabled dashboard. All covers use a 4:3 ratio, metadata follows a consistent order, and the grid becomes one column below 768 px. The all-projects link is inside the section.
- The production build passes. A static check of the built homepage confirms four distinct project cards, four responsive covers, and all 25 project-link, image, and sculpture-texture references resolve. Existing card-reveal and project-navigation hooks are retained.
- Browser visual verification remains pending due to the earlier automatic approval rejection for exhausted workspace credits. No new viewport or accessibility measurements are claimed for this layout.

## Theme-switch animation

- Full motion uses a feathered radial reveal from the theme button with a short halo and sun/moon animation: 1.05 seconds on desktop and 0.88 seconds on mobile. Unsupported snapshot/masking APIs use a 0.36-second palette fade. Reduced motion applies the palette immediately.
- Twelve new Node checks cover successful completion, rapid clicks and stale callbacks, missing browser features, throwing/rejected snapshot APIs, hidden tabs, active loader behavior, scroll/resize/navigation/print interruption, Escape, live motion changes, and a stalled API timeout. These use event and snapshot doubles; they do not verify browser rendering.
- `npm run check` and `npm run build` pass on 14 September 2026. The build retains the existing warning for the dynamically loaded Three.js chunk.
- Browser verification of the new animation is pending. Automatic approval review rejected the local Chrome test launch because the workspace was out of credits. No alternative browser launch was used to bypass that rejection. The browser and screenshot results below describe the earlier static theme implementation.

## Light theme

- Added a persistent light/dark button beside the motion control on every page. Dark remains the default. Saved light mode is restored by the pre-paint bootstrap, including when the app bundle cannot load. Tab synchronization, navigation, reload, history, unavailable storage, and JavaScript-disabled fallback are covered.
- The 50 existing browser checks pass after the theme addition. All 24 new theme checks are validated: 23 passed initially, and CHIP-2’s small accent labels passed after a contrast adjustment. Automated axe checks cover all 18 pages in light mode plus the menu and media dialog. Theme changes retain the active motion setting and the existing canvas/pinning; switching motion retains the theme.
- Light layouts and the adjacent footer controls fit 360, 390, 834, 1440, and 1920 px. Desktop and mobile screenshots of Home, About, Contact, Résumé, Work, Playground, Blog, an article, a case study, and the navigation menu were reviewed under `artifacts/theme-review/`.
- The light loader still holds content for five seconds with the app bundle blocked. Browser print keeps the résumé white and removes footer controls. No résumé content or PDF asset was changed.
- The palette is defined in `src/theme.css`: warm ivory, ink text, sage surfaces, readable project accents, and original electric-lime artwork/filled controls. The site is not recolored through an image-inversion filter.

## Profile update

- Current role is Product Designer at Khushi Baby from April 2026. The historical Senior UI/UX Designer role ends in March 2026. Confirmed email, phone, LinkedIn, education, languages, and tools appear in the relevant pages.
- Seven actual project summaries replace the original concept projects. The next-project loop, filters, menu previews, gallery assets, explorer IDs, homepage covers, and sculpture textures use the updated routes. Old production URLs redirect to the new project pages.
- Reviewed About, Home, Work, Contact, Résumé, and long project titles at 390 and 1440 px. The About portrait is a responsive WebP crop of Sumit's existing image. All sourced image elements load successfully; horizontal document width stays within the viewport.
- The downloadable résumé uses a two-page A4 print layout with text-selectable content and active confirmed contact links. Both pages are rendered and visually checked. Its source is `resume/index.html`; `npm run resume:pdf` regenerates the downloadable copy.
- Original résumé files are unchanged. Project drawings are labeled workflow illustrations and avoid transferring personal records from healthcare captures. Figures preserve the supplied source context; no invented testimonial or employment claim was added. See `docs/PROFILE-SOURCES.md`.
- Browser interaction and accessibility checks cover the real contact email draft, résumé download link, new project routes, and all previous motion/fallback behavior. No message is sent by the contact form itself.

## Motion update

- Verified the mandatory five-second hold on visits 1, 6, and 11, suppression on intervening visits, and persistence across refreshes and a new tab. Page clicks and Escape cannot skip the hold. Each full page load counts as a visit; the count stays in local browser storage across sessions.
- Verified the independent five-second release with both the app bundle and local storage deliberately blocked. Reduced motion displays a static five-second loader; changing the motion preference during the hold does not dismiss it. Repeated menu open/Escape cycles, restored focus, focused project previews, and a menu accessibility audit remain covered.
- Verified accordion reversal during an active transition, Enter/Space operation, mobile resizing, interrupted closing, and JavaScript-disabled native disclosure behavior.
- Verified scroll progress, collection filtering after reveals, reduced-motion cleanup, page entrances across every page type (including 404), and back/forward navigation.
- Reviewed desktop and mobile screenshots of the loader and menu, plus the expanded contact FAQ. Images are under `artifacts/motion/`; `node scripts/motion-shots.mjs` regenerates them with local Chrome.
- Loader and page progress add no layout space. The loader covers the viewport and temporarily makes the page inert, releasing interaction at its deadline independently of GSAP. Its copy is not a simulated asset-loading percentage. Case-cover parallax includes enough image overscan to avoid exposed edges.

## Mobile Lighthouse

The reports use Lighthouse's mobile emulation against `http://127.0.0.1:4173`. Results are local lab measurements, not field data. **The scores below predate the mandatory five-second loader and profile personalization.** That latest requested change deliberately delays page interaction on visits 1, 6, 11, and so on; these earlier scores should not be presented as measurements of the new loading policy.

| Page     | Performance | Accessibility | Best practices | SEO |
| -------- | ----------: | ------------: | -------------: | --: |
| Homepage |          96 |           100 |            100 |  66 |
| About    |          98 |           100 |            100 |  63 |
| Contact  |          98 |           100 |            100 |  63 |
| Résumé   |          94 |           100 |            100 |  63 |
| Work     |          96 |           100 |            100 |  66 |
| Blog     |          96 |           100 |            100 |  63 |
| Article  |          99 |           100 |            100 |  63 |

Homepage, About, and Contact were measured after the initial motion update (96, 98, and 98 performance respectively, with 100 accessibility). The remaining rows retain the earlier page-expansion measurements. The performance and automated accessibility targets passed on those measured builds. Re-run Lighthouse for the current mandatory-loader behavior before making updated performance claims. The SEO target remains pending: the preview intentionally uses `noindex` and blocks crawlers. The other scored SEO checks passed. Review the project presentation and article copy, configure the real URL, and run a release audit before enabling indexing. Audit reports are in `artifacts/audits/`.

The résumé performance score passes the 90+ target; its latest measured layout shift is 0.129, which leaves room for further font/layout tuning with the final personal content.

The desktop-only Three.js chunk exceeds Vite's generic 500 KB warning threshold. It is dynamically imported and excluded from mobile/reduced-motion startup; essential content is static HTML.

## Publication status and remaining content

GitHub Pages automation is prepared. No public deployment has been created. The release check still requires preview mode to be disabled, a real origin, and the remaining publication-review flags.

Identity, About copy, contact details, work history, education, skills, seven project summaries, and the updated résumé are integrated. Before publication, review project illustrations/qualified results and the authored blog notes. The homepage omits testimonials until authentic attributed quotes are supplied. Review `site.config.json` and the release steps in `README.md`.

Safari, Firefox, physical devices, and manual screen-reader sessions were not exercised in this environment. Automated accessibility scores do not replace that review. Native navigation is the fallback when View Transitions are unsupported.
